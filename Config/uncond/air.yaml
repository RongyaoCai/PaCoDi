model:
  target: Models.PaCoDi.diffusion_ddpm.PaCoDi_ddpm
  params:
    seq_length: 256
    feature_size: 13
    timesteps: 2500
    sampling_timesteps: 2500
    beta_schedule: 'cosine'
    emb_size: 256
    patch_size: 4
    num_layers: 6

solver:
  base_lr: 1.0e-5
  max_epochs: 40000
  results_folder: ./Checkpoints_air
  gradient_accumulate_every: 2
  save_cycle: 4000  # max_epochs // 10
  ema:
    decay: 0.995
    update_interval: 10
  
  scheduler:
    target: engine.lr_sch.ReduceLROnPlateauWithWarmup
    params:
      factor: 0.5
      patience: 4000
      min_lr: 1.0e-5
      threshold: 1.0e-1
      threshold_mode: rel
      warmup_lr: 8.0e-4
      warmup: 500 
      verbose: False

dataloader:
  train_dataset:
    target: Utils.Data_utils.real_datasets.CustomDataset
    params:
      name: air
      proportion: 1.0  # Set to rate < 1 if training conditional generation
      data_root: ./Data/datasets/AirQuality_clean.csv
      window: 256  # seq_length
      save2npy: True
      neg_one_to_one: True
      seed: 123
      period: train

  test_dataset:
    target: Utils.Data_utils.real_datasets.CustomDataset
    params:
      name: air
      proportion: 0.9  # rate
      data_root: ./Data/datasets/AirQuality_clean.csv
      window: 256  # seq_length
      save2npy: True
      neg_one_to_one: True
      seed: 123
      period: test
      style: separate
      distribution: geometric
    coefficient: 1.0e-2
    step_size: 5.0e-2
    sampling_steps: 200

  batch_size: 128
  sample_size: 256
  shuffle: True